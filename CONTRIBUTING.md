# Contributing

Keep pull requests small and tied to one milestone or issue.

```bash
npm run test
npm run typecheck
npm run build
```

Use conventional commit subjects (`feat:`, `fix:`, `docs:`). Do not add dependencies when a
browser or existing project API covers the need. Never commit exported study data or secrets.
